#include "main.h"
#include <stdio.h>
/**
  * _strlen - returns length of string
  *
  * Return: (0)
  */
int _strlen(char *s)
{
	int str_length = 0'

		for ( int i = 0; str[i] != '\0'; i++)
		{
			str_length++;
		}
	return (str_length);
}
